[DEFAULT]
debug = false
smtp_server = localhost
error_email_from = paste@localhost

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 5000

[app:main]
use = config:/usr/lib/ckan/default/src/ckan/test-core.ini
ckan.plugins = fluent scheming_datasets schema_field_groups odm_dataset_ext odm_dataset_resource odm_mimu_harvester

scheming.dataset_schemas = ckanext.odm_dataset_ext:odm_dataset_schema.json
scheming.presets = ckanext.fluent:presets.json ckanext.odm_dataset_ext:odm_dataset_presets.json
scheming.dataset_fallback = false
ckanext.issues.review_system=True
ckanext.issues.new.description_link=https://test.com
ckanext-odm_dataset_ext.country_code = *


# Logging configuration
[loggers]
keys = root, ckan, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_ckan]
qualname = ckan
handlers =
level = INFO

[logger_sqlalchemy]
handlers =
qualname = sqlalchemy.engine
level = WARN

[handler_console]
class = StreamHandler
args = (sys.stdout,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s] %(message)s